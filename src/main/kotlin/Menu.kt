enum class Menu(val index: Int, val text: String) {
    ALL_DATA_TYPES(0, "Show all available data types"),
    TRY_ALL_DATA_TYPES(1, "Try all data types"),
    CONTROL_FLOWS(2, "Try control flows")
}